let idade = 38;
let pais = `Brasil`;

// condicional ternario: sem parenteses;
let e_Adulto = idade >= 18 ? `Voce é maior de idade` : `Voce é menor de idade`;

// condicional ternario: com parenteses;
nacionalidade = (pais === `Brasil` ? `Voce é brasileiro` : `Voce não é brasileiro`);

console.log(e_Adulto);
console.log(nacionalidade);